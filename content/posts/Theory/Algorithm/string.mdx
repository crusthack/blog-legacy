---
title: '문자열 처리'
date: '2026-06-01'
description: ''
titleSlide: true
---

# 문자열 처리 개요

# KMP Algorithm O(N + M)
# Rabin-Karp Algorithm O(N + M)
# Z Algorithm O(N)
# Boyer-Moore Algorithm O(N / M)
# Aho-Corasick Algorithm O(N + M + Z)
# Manacher Algorithm O(N)
# Trie Data Structure O(L)
# Suffix Tree O(N)
# Suffix Array O(N log N)
# Rolling Hash O(N)